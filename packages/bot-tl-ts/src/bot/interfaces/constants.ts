
export enum EParseMode {
  Markdown = 'Markdown',
  MarkdownV2 = 'MarkdownV2',
  HTML = 'HTML'
}

export enum EChatType {
  Group = 'group',
  SuperGroup = 'supergroup',
  Channel = 'channel',
  Private = 'private'
}
